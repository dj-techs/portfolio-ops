# scripts/trending_scan.py and scripts/prune_stale_trending.py use the Python
# standard library only (urllib, json, re, datetime). No external deps needed
# at the current implementation. Future sessions may add: anthropic, httpx,
# feedparser, beautifulsoup4 if richer parsing or SDK ergonomics are wanted.
